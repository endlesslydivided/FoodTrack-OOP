<UserControl x:Class="FoodTrack.Views.Pages.TodayResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FoodTrack.Views.Pages"
             mc:Ignorable="d" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cpb="clr-namespace:FoodTrack.UI"
            d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!--<cpb:AngleToPointConverter x:Key="prConverter" />
        <cpb:AngleToIsLargeConverter x:Key="isLargeConverter" />-->

        <Style x:Key="circularProgressBar" TargetType="cpb:CircularProgressBar">
            <Setter Property="Value" Value="10" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="StrokeThickness" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="cpb:CircularProgressBar">
                        <Canvas Width="100" Height="100">
                            <Ellipse
                                Width="100"
                                Height="100"
                                Stroke="LightGray"
                                StrokeThickness="1" />

                            <Path Stroke="{TemplateBinding Background}" StrokeThickness="{TemplateBinding StrokeThickness}">
                                <Path.Data>
                                    <PathGeometry>
                                        <PathFigure x:Name="fig" StartPoint="50,0">
                                            <ArcSegment
                                                IsLargeArc="{Binding Path=Angle, Converter={StaticResource isLargeConverter}, RelativeSource={RelativeSource FindAncestor, AncestorType=ProgressBar}}"
                                                Point="{Binding Path=Angle, Converter={StaticResource prConverter}, RelativeSource={RelativeSource FindAncestor, AncestorType=ProgressBar}}"
                                                RotationAngle="0"
                                                Size="50,50"
                                                SweepDirection="Clockwise" />
                                        </PathFigure>
                                    </PathGeometry>
                                </Path.Data>
                            </Path>
                            <Border Width="100" Height="100">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="{TemplateBinding FontSize}"
                                    Foreground="Gray"
                                    Text="{Binding Path=Value, StringFormat={}%{0}, RelativeSource={RelativeSource TemplatedParent}}" />
                            </Border>
                        </Canvas>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <cpb:CircularProgressBar></cpb:CircularProgressBar>
    </Grid>
</UserControl>

